// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.AttendanceStatus;
import com.school.base.domain.AttendanceStatusDataOnDemand;
import com.school.base.domain.SchoolSession;
import com.school.base.domain.SchoolSessionDataOnDemand;
import com.school.base.domain.Staff;
import com.school.base.domain.StaffAttendance;
import com.school.base.domain.StaffAttendanceDataOnDemand;
import com.school.base.domain.StaffDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect StaffAttendanceDataOnDemand_Roo_DataOnDemand {
    
    declare @type: StaffAttendanceDataOnDemand: @Component;
    
    private Random StaffAttendanceDataOnDemand.rnd = new SecureRandom();
    
    private List<StaffAttendance> StaffAttendanceDataOnDemand.data;
    
    @Autowired
    AttendanceStatusDataOnDemand StaffAttendanceDataOnDemand.attendanceStatusDataOnDemand;
    
    @Autowired
    StaffDataOnDemand StaffAttendanceDataOnDemand.staffDataOnDemand;
    
    @Autowired
    SchoolSessionDataOnDemand StaffAttendanceDataOnDemand.schoolSessionDataOnDemand;
    
    public StaffAttendance StaffAttendanceDataOnDemand.getNewTransientStaffAttendance(int index) {
        StaffAttendance obj = new StaffAttendance();
        setAttendanceDate(obj, index);
        setAttendanceStatusId(obj, index);
        setAuditCreatedDtTime(obj, index);
        setAuditModifiedDtTime(obj, index);
        setAuditUserId(obj, index);
        setSchoolSessionId(obj, index);
        setStaffId(obj, index);
        return obj;
    }
    
    public void StaffAttendanceDataOnDemand.setAttendanceDate(StaffAttendance obj, int index) {
        Date attendanceDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setAttendanceDate(attendanceDate);
    }
    
    public void StaffAttendanceDataOnDemand.setAttendanceStatusId(StaffAttendance obj, int index) {
        AttendanceStatus attendanceStatusId = attendanceStatusDataOnDemand.getRandomAttendanceStatus();
        obj.setAttendanceStatusId(attendanceStatusId);
    }
    
    public void StaffAttendanceDataOnDemand.setAuditCreatedDtTime(StaffAttendance obj, int index) {
        Calendar auditCreatedDtTime = Calendar.getInstance();
        obj.setAuditCreatedDtTime(auditCreatedDtTime);
    }
    
    public void StaffAttendanceDataOnDemand.setAuditModifiedDtTime(StaffAttendance obj, int index) {
        Calendar auditModifiedDtTime = Calendar.getInstance();
        obj.setAuditModifiedDtTime(auditModifiedDtTime);
    }
    
    public void StaffAttendanceDataOnDemand.setAuditUserId(StaffAttendance obj, int index) {
        Staff auditUserId = staffDataOnDemand.getRandomStaff();
        obj.setAuditUserId(auditUserId);
    }
    
    public void StaffAttendanceDataOnDemand.setSchoolSessionId(StaffAttendance obj, int index) {
        SchoolSession schoolSessionId = schoolSessionDataOnDemand.getRandomSchoolSession();
        obj.setSchoolSessionId(schoolSessionId);
    }
    
    public void StaffAttendanceDataOnDemand.setStaffId(StaffAttendance obj, int index) {
        Staff staffId = staffDataOnDemand.getRandomStaff();
        obj.setStaffId(staffId);
    }
    
    public StaffAttendance StaffAttendanceDataOnDemand.getSpecificStaffAttendance(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        StaffAttendance obj = data.get(index);
        Integer id = obj.getStaffAttendanceId();
        return StaffAttendance.findStaffAttendance(id);
    }
    
    public StaffAttendance StaffAttendanceDataOnDemand.getRandomStaffAttendance() {
        init();
        StaffAttendance obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getStaffAttendanceId();
        return StaffAttendance.findStaffAttendance(id);
    }
    
    public boolean StaffAttendanceDataOnDemand.modifyStaffAttendance(StaffAttendance obj) {
        return false;
    }
    
    public void StaffAttendanceDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = StaffAttendance.findStaffAttendanceEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'StaffAttendance' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<StaffAttendance>();
        for (int i = 0; i < 10; i++) {
            StaffAttendance obj = getNewTransientStaffAttendance(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
