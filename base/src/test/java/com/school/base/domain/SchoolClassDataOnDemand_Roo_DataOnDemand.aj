// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.School;
import com.school.base.domain.SchoolClass;
import com.school.base.domain.SchoolClassDataOnDemand;
import com.school.base.domain.SchoolDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect SchoolClassDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SchoolClassDataOnDemand: @Component;
    
    private Random SchoolClassDataOnDemand.rnd = new SecureRandom();
    
    private List<SchoolClass> SchoolClassDataOnDemand.data;
    
    @Autowired
    SchoolDataOnDemand SchoolClassDataOnDemand.schoolDataOnDemand;
    
    public SchoolClass SchoolClassDataOnDemand.getNewTransientSchoolClass(int index) {
        SchoolClass obj = new SchoolClass();
        setClassCode(obj, index);
        setClassName(obj, index);
        setSchoolId(obj, index);
        return obj;
    }
    
    public void SchoolClassDataOnDemand.setClassCode(SchoolClass obj, int index) {
        String classCode = "classCode_" + index;
        if (classCode.length() > 50) {
            classCode = classCode.substring(0, 50);
        }
        obj.setClassCode(classCode);
    }
    
    public void SchoolClassDataOnDemand.setClassName(SchoolClass obj, int index) {
        String className = "className_" + index;
        if (className.length() > 100) {
            className = className.substring(0, 100);
        }
        obj.setClassName(className);
    }
    
    public void SchoolClassDataOnDemand.setSchoolId(SchoolClass obj, int index) {
        School schoolId = schoolDataOnDemand.getRandomSchool();
        obj.setSchoolId(schoolId);
    }
    
    public SchoolClass SchoolClassDataOnDemand.getSpecificSchoolClass(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        SchoolClass obj = data.get(index);
        Integer id = obj.getSchoolClassId();
        return SchoolClass.findSchoolClass(id);
    }
    
    public SchoolClass SchoolClassDataOnDemand.getRandomSchoolClass() {
        init();
        SchoolClass obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getSchoolClassId();
        return SchoolClass.findSchoolClass(id);
    }
    
    public boolean SchoolClassDataOnDemand.modifySchoolClass(SchoolClass obj) {
        return false;
    }
    
    public void SchoolClassDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = SchoolClass.findSchoolClassEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'SchoolClass' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<SchoolClass>();
        for (int i = 0; i < 10; i++) {
            SchoolClass obj = getNewTransientSchoolClass(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
