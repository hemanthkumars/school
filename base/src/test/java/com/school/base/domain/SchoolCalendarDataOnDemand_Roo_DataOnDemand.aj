// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.School;
import com.school.base.domain.SchoolCalendar;
import com.school.base.domain.SchoolCalendarDataOnDemand;
import com.school.base.domain.SchoolDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect SchoolCalendarDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SchoolCalendarDataOnDemand: @Component;
    
    private Random SchoolCalendarDataOnDemand.rnd = new SecureRandom();
    
    private List<SchoolCalendar> SchoolCalendarDataOnDemand.data;
    
    @Autowired
    SchoolDataOnDemand SchoolCalendarDataOnDemand.schoolDataOnDemand;
    
    public SchoolCalendar SchoolCalendarDataOnDemand.getNewTransientSchoolCalendar(int index) {
        SchoolCalendar obj = new SchoolCalendar();
        setEventDate(obj, index);
        setProgram(obj, index);
        setSchoolId(obj, index);
        setTitle(obj, index);
        return obj;
    }
    
    public void SchoolCalendarDataOnDemand.setEventDate(SchoolCalendar obj, int index) {
        Date eventDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setEventDate(eventDate);
    }
    
    public void SchoolCalendarDataOnDemand.setProgram(SchoolCalendar obj, int index) {
        String program = "program_" + index;
        if (program.length() > 500) {
            program = program.substring(0, 500);
        }
        obj.setProgram(program);
    }
    
    public void SchoolCalendarDataOnDemand.setSchoolId(SchoolCalendar obj, int index) {
        School schoolId = schoolDataOnDemand.getRandomSchool();
        obj.setSchoolId(schoolId);
    }
    
    public void SchoolCalendarDataOnDemand.setTitle(SchoolCalendar obj, int index) {
        String title = "title_" + index;
        if (title.length() > 200) {
            title = title.substring(0, 200);
        }
        obj.setTitle(title);
    }
    
    public SchoolCalendar SchoolCalendarDataOnDemand.getSpecificSchoolCalendar(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        SchoolCalendar obj = data.get(index);
        Integer id = obj.getSchoolCalendarId();
        return SchoolCalendar.findSchoolCalendar(id);
    }
    
    public SchoolCalendar SchoolCalendarDataOnDemand.getRandomSchoolCalendar() {
        init();
        SchoolCalendar obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getSchoolCalendarId();
        return SchoolCalendar.findSchoolCalendar(id);
    }
    
    public boolean SchoolCalendarDataOnDemand.modifySchoolCalendar(SchoolCalendar obj) {
        return false;
    }
    
    public void SchoolCalendarDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = SchoolCalendar.findSchoolCalendarEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'SchoolCalendar' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<SchoolCalendar>();
        for (int i = 0; i < 10; i++) {
            SchoolCalendar obj = getNewTransientSchoolCalendar(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
