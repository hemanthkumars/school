// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.ClassExamination;
import com.school.base.domain.ClassExaminationDataOnDemand;
import com.school.base.domain.ClassExaminationIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ClassExaminationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ClassExaminationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ClassExaminationIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ClassExaminationIntegrationTest: @Transactional;
    
    @Autowired
    ClassExaminationDataOnDemand ClassExaminationIntegrationTest.dod;
    
    @Test
    public void ClassExaminationIntegrationTest.testCountClassExaminations() {
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to initialize correctly", dod.getRandomClassExamination());
        long count = ClassExamination.countClassExaminations();
        Assert.assertTrue("Counter for 'ClassExamination' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ClassExaminationIntegrationTest.testFindClassExamination() {
        ClassExamination obj = dod.getRandomClassExamination();
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to initialize correctly", obj);
        Integer id = obj.getClassExaminationId();
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to provide an identifier", id);
        obj = ClassExamination.findClassExamination(id);
        Assert.assertNotNull("Find method for 'ClassExamination' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ClassExamination' returned the incorrect identifier", id, obj.getClassExaminationId());
    }
    
    @Test
    public void ClassExaminationIntegrationTest.testFindAllClassExaminations() {
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to initialize correctly", dod.getRandomClassExamination());
        long count = ClassExamination.countClassExaminations();
        Assert.assertTrue("Too expensive to perform a find all test for 'ClassExamination', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ClassExamination> result = ClassExamination.findAllClassExaminations();
        Assert.assertNotNull("Find all method for 'ClassExamination' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ClassExamination' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ClassExaminationIntegrationTest.testFindClassExaminationEntries() {
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to initialize correctly", dod.getRandomClassExamination());
        long count = ClassExamination.countClassExaminations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ClassExamination> result = ClassExamination.findClassExaminationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ClassExamination' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ClassExamination' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ClassExaminationIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to initialize correctly", dod.getRandomClassExamination());
        ClassExamination obj = dod.getNewTransientClassExamination(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ClassExamination' identifier to be null", obj.getClassExaminationId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ClassExamination' identifier to no longer be null", obj.getClassExaminationId());
    }
    
    @Test
    public void ClassExaminationIntegrationTest.testRemove() {
        ClassExamination obj = dod.getRandomClassExamination();
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to initialize correctly", obj);
        Integer id = obj.getClassExaminationId();
        Assert.assertNotNull("Data on demand for 'ClassExamination' failed to provide an identifier", id);
        obj = ClassExamination.findClassExamination(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ClassExamination' with identifier '" + id + "'", ClassExamination.findClassExamination(id));
    }
    
}
