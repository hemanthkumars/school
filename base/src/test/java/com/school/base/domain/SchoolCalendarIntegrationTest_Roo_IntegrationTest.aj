// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.SchoolCalendar;
import com.school.base.domain.SchoolCalendarDataOnDemand;
import com.school.base.domain.SchoolCalendarIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SchoolCalendarIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SchoolCalendarIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SchoolCalendarIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SchoolCalendarIntegrationTest: @Transactional;
    
    @Autowired
    SchoolCalendarDataOnDemand SchoolCalendarIntegrationTest.dod;
    
    @Test
    public void SchoolCalendarIntegrationTest.testCountSchoolCalendars() {
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to initialize correctly", dod.getRandomSchoolCalendar());
        long count = SchoolCalendar.countSchoolCalendars();
        Assert.assertTrue("Counter for 'SchoolCalendar' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SchoolCalendarIntegrationTest.testFindSchoolCalendar() {
        SchoolCalendar obj = dod.getRandomSchoolCalendar();
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to initialize correctly", obj);
        Integer id = obj.getSchoolCalendarId();
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to provide an identifier", id);
        obj = SchoolCalendar.findSchoolCalendar(id);
        Assert.assertNotNull("Find method for 'SchoolCalendar' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SchoolCalendar' returned the incorrect identifier", id, obj.getSchoolCalendarId());
    }
    
    @Test
    public void SchoolCalendarIntegrationTest.testFindAllSchoolCalendars() {
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to initialize correctly", dod.getRandomSchoolCalendar());
        long count = SchoolCalendar.countSchoolCalendars();
        Assert.assertTrue("Too expensive to perform a find all test for 'SchoolCalendar', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SchoolCalendar> result = SchoolCalendar.findAllSchoolCalendars();
        Assert.assertNotNull("Find all method for 'SchoolCalendar' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SchoolCalendar' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SchoolCalendarIntegrationTest.testFindSchoolCalendarEntries() {
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to initialize correctly", dod.getRandomSchoolCalendar());
        long count = SchoolCalendar.countSchoolCalendars();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SchoolCalendar> result = SchoolCalendar.findSchoolCalendarEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SchoolCalendar' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SchoolCalendar' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SchoolCalendarIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to initialize correctly", dod.getRandomSchoolCalendar());
        SchoolCalendar obj = dod.getNewTransientSchoolCalendar(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SchoolCalendar' identifier to be null", obj.getSchoolCalendarId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'SchoolCalendar' identifier to no longer be null", obj.getSchoolCalendarId());
    }
    
    @Test
    public void SchoolCalendarIntegrationTest.testRemove() {
        SchoolCalendar obj = dod.getRandomSchoolCalendar();
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to initialize correctly", obj);
        Integer id = obj.getSchoolCalendarId();
        Assert.assertNotNull("Data on demand for 'SchoolCalendar' failed to provide an identifier", id);
        obj = SchoolCalendar.findSchoolCalendar(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SchoolCalendar' with identifier '" + id + "'", SchoolCalendar.findSchoolCalendar(id));
    }
    
}
