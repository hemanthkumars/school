// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.AcademicYear;
import com.school.base.domain.AcademicYearDataOnDemand;
import com.school.base.domain.AcademicYearIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AcademicYearIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AcademicYearIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AcademicYearIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AcademicYearIntegrationTest: @Transactional;
    
    @Autowired
    AcademicYearDataOnDemand AcademicYearIntegrationTest.dod;
    
    @Test
    public void AcademicYearIntegrationTest.testCountAcademicYears() {
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to initialize correctly", dod.getRandomAcademicYear());
        long count = AcademicYear.countAcademicYears();
        Assert.assertTrue("Counter for 'AcademicYear' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AcademicYearIntegrationTest.testFindAcademicYear() {
        AcademicYear obj = dod.getRandomAcademicYear();
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to initialize correctly", obj);
        Integer id = obj.getAcademicYearId();
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to provide an identifier", id);
        obj = AcademicYear.findAcademicYear(id);
        Assert.assertNotNull("Find method for 'AcademicYear' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AcademicYear' returned the incorrect identifier", id, obj.getAcademicYearId());
    }
    
    @Test
    public void AcademicYearIntegrationTest.testFindAllAcademicYears() {
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to initialize correctly", dod.getRandomAcademicYear());
        long count = AcademicYear.countAcademicYears();
        Assert.assertTrue("Too expensive to perform a find all test for 'AcademicYear', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AcademicYear> result = AcademicYear.findAllAcademicYears();
        Assert.assertNotNull("Find all method for 'AcademicYear' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AcademicYear' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AcademicYearIntegrationTest.testFindAcademicYearEntries() {
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to initialize correctly", dod.getRandomAcademicYear());
        long count = AcademicYear.countAcademicYears();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AcademicYear> result = AcademicYear.findAcademicYearEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AcademicYear' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AcademicYear' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AcademicYearIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to initialize correctly", dod.getRandomAcademicYear());
        AcademicYear obj = dod.getNewTransientAcademicYear(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AcademicYear' identifier to be null", obj.getAcademicYearId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'AcademicYear' identifier to no longer be null", obj.getAcademicYearId());
    }
    
    @Test
    public void AcademicYearIntegrationTest.testRemove() {
        AcademicYear obj = dod.getRandomAcademicYear();
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to initialize correctly", obj);
        Integer id = obj.getAcademicYearId();
        Assert.assertNotNull("Data on demand for 'AcademicYear' failed to provide an identifier", id);
        obj = AcademicYear.findAcademicYear(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AcademicYear' with identifier '" + id + "'", AcademicYear.findAcademicYear(id));
    }
    
}
