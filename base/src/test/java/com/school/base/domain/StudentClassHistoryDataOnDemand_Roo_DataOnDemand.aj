// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.SchoolClassSection;
import com.school.base.domain.SchoolClassSectionDataOnDemand;
import com.school.base.domain.Student;
import com.school.base.domain.StudentClassHistory;
import com.school.base.domain.StudentClassHistoryDataOnDemand;
import com.school.base.domain.StudentDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect StudentClassHistoryDataOnDemand_Roo_DataOnDemand {
    
    declare @type: StudentClassHistoryDataOnDemand: @Component;
    
    private Random StudentClassHistoryDataOnDemand.rnd = new SecureRandom();
    
    private List<StudentClassHistory> StudentClassHistoryDataOnDemand.data;
    
    @Autowired
    SchoolClassSectionDataOnDemand StudentClassHistoryDataOnDemand.schoolClassSectionDataOnDemand;
    
    @Autowired
    StudentDataOnDemand StudentClassHistoryDataOnDemand.studentDataOnDemand;
    
    public StudentClassHistory StudentClassHistoryDataOnDemand.getNewTransientStudentClassHistory(int index) {
        StudentClassHistory obj = new StudentClassHistory();
        setAuditDtTime(obj, index);
        setSchoolClassSectionId(obj, index);
        setStudentId(obj, index);
        return obj;
    }
    
    public void StudentClassHistoryDataOnDemand.setAuditDtTime(StudentClassHistory obj, int index) {
        Calendar auditDtTime = Calendar.getInstance();
        obj.setAuditDtTime(auditDtTime);
    }
    
    public void StudentClassHistoryDataOnDemand.setSchoolClassSectionId(StudentClassHistory obj, int index) {
        SchoolClassSection schoolClassSectionId = schoolClassSectionDataOnDemand.getRandomSchoolClassSection();
        obj.setSchoolClassSectionId(schoolClassSectionId);
    }
    
    public void StudentClassHistoryDataOnDemand.setStudentId(StudentClassHistory obj, int index) {
        Student studentId = studentDataOnDemand.getRandomStudent();
        obj.setStudentId(studentId);
    }
    
    public StudentClassHistory StudentClassHistoryDataOnDemand.getSpecificStudentClassHistory(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        StudentClassHistory obj = data.get(index);
        Integer id = obj.getStudentClassHistory();
        return StudentClassHistory.findStudentClassHistory(id);
    }
    
    public StudentClassHistory StudentClassHistoryDataOnDemand.getRandomStudentClassHistory() {
        init();
        StudentClassHistory obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getStudentClassHistory();
        return StudentClassHistory.findStudentClassHistory(id);
    }
    
    public boolean StudentClassHistoryDataOnDemand.modifyStudentClassHistory(StudentClassHistory obj) {
        return false;
    }
    
    public void StudentClassHistoryDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = StudentClassHistory.findStudentClassHistoryEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'StudentClassHistory' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<StudentClassHistory>();
        for (int i = 0; i < 10; i++) {
            StudentClassHistory obj = getNewTransientStudentClassHistory(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
