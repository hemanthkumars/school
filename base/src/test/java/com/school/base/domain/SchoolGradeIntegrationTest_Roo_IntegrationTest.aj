// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.SchoolGrade;
import com.school.base.domain.SchoolGradeDataOnDemand;
import com.school.base.domain.SchoolGradeIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SchoolGradeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SchoolGradeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SchoolGradeIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SchoolGradeIntegrationTest: @Transactional;
    
    @Autowired
    SchoolGradeDataOnDemand SchoolGradeIntegrationTest.dod;
    
    @Test
    public void SchoolGradeIntegrationTest.testCountSchoolGrades() {
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to initialize correctly", dod.getRandomSchoolGrade());
        long count = SchoolGrade.countSchoolGrades();
        Assert.assertTrue("Counter for 'SchoolGrade' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SchoolGradeIntegrationTest.testFindSchoolGrade() {
        SchoolGrade obj = dod.getRandomSchoolGrade();
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to initialize correctly", obj);
        Integer id = obj.getSchoolGradeId();
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to provide an identifier", id);
        obj = SchoolGrade.findSchoolGrade(id);
        Assert.assertNotNull("Find method for 'SchoolGrade' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SchoolGrade' returned the incorrect identifier", id, obj.getSchoolGradeId());
    }
    
    @Test
    public void SchoolGradeIntegrationTest.testFindAllSchoolGrades() {
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to initialize correctly", dod.getRandomSchoolGrade());
        long count = SchoolGrade.countSchoolGrades();
        Assert.assertTrue("Too expensive to perform a find all test for 'SchoolGrade', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SchoolGrade> result = SchoolGrade.findAllSchoolGrades();
        Assert.assertNotNull("Find all method for 'SchoolGrade' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SchoolGrade' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SchoolGradeIntegrationTest.testFindSchoolGradeEntries() {
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to initialize correctly", dod.getRandomSchoolGrade());
        long count = SchoolGrade.countSchoolGrades();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SchoolGrade> result = SchoolGrade.findSchoolGradeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SchoolGrade' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SchoolGrade' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SchoolGradeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to initialize correctly", dod.getRandomSchoolGrade());
        SchoolGrade obj = dod.getNewTransientSchoolGrade(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SchoolGrade' identifier to be null", obj.getSchoolGradeId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'SchoolGrade' identifier to no longer be null", obj.getSchoolGradeId());
    }
    
    @Test
    public void SchoolGradeIntegrationTest.testRemove() {
        SchoolGrade obj = dod.getRandomSchoolGrade();
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to initialize correctly", obj);
        Integer id = obj.getSchoolGradeId();
        Assert.assertNotNull("Data on demand for 'SchoolGrade' failed to provide an identifier", id);
        obj = SchoolGrade.findSchoolGrade(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SchoolGrade' with identifier '" + id + "'", SchoolGrade.findSchoolGrade(id));
    }
    
}
