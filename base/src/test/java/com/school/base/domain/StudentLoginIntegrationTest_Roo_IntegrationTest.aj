// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.school.base.domain;

import com.school.base.domain.StudentLogin;
import com.school.base.domain.StudentLoginDataOnDemand;
import com.school.base.domain.StudentLoginIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect StudentLoginIntegrationTest_Roo_IntegrationTest {
    
    declare @type: StudentLoginIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: StudentLoginIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: StudentLoginIntegrationTest: @Transactional;
    
    @Autowired
    StudentLoginDataOnDemand StudentLoginIntegrationTest.dod;
    
    @Test
    public void StudentLoginIntegrationTest.testCountStudentLogins() {
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to initialize correctly", dod.getRandomStudentLogin());
        long count = StudentLogin.countStudentLogins();
        Assert.assertTrue("Counter for 'StudentLogin' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void StudentLoginIntegrationTest.testFindStudentLogin() {
        StudentLogin obj = dod.getRandomStudentLogin();
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to initialize correctly", obj);
        Integer id = obj.getStudentLoginId();
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to provide an identifier", id);
        obj = StudentLogin.findStudentLogin(id);
        Assert.assertNotNull("Find method for 'StudentLogin' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'StudentLogin' returned the incorrect identifier", id, obj.getStudentLoginId());
    }
    
    @Test
    public void StudentLoginIntegrationTest.testFindAllStudentLogins() {
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to initialize correctly", dod.getRandomStudentLogin());
        long count = StudentLogin.countStudentLogins();
        Assert.assertTrue("Too expensive to perform a find all test for 'StudentLogin', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<StudentLogin> result = StudentLogin.findAllStudentLogins();
        Assert.assertNotNull("Find all method for 'StudentLogin' illegally returned null", result);
        Assert.assertTrue("Find all method for 'StudentLogin' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void StudentLoginIntegrationTest.testFindStudentLoginEntries() {
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to initialize correctly", dod.getRandomStudentLogin());
        long count = StudentLogin.countStudentLogins();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<StudentLogin> result = StudentLogin.findStudentLoginEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'StudentLogin' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'StudentLogin' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void StudentLoginIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to initialize correctly", dod.getRandomStudentLogin());
        StudentLogin obj = dod.getNewTransientStudentLogin(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'StudentLogin' identifier to be null", obj.getStudentLoginId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'StudentLogin' identifier to no longer be null", obj.getStudentLoginId());
    }
    
    @Test
    public void StudentLoginIntegrationTest.testRemove() {
        StudentLogin obj = dod.getRandomStudentLogin();
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to initialize correctly", obj);
        Integer id = obj.getStudentLoginId();
        Assert.assertNotNull("Data on demand for 'StudentLogin' failed to provide an identifier", id);
        obj = StudentLogin.findStudentLogin(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'StudentLogin' with identifier '" + id + "'", StudentLogin.findStudentLogin(id));
    }
    
}
